import React, { Component } from "react"

import styles from "./receipt.module.scss"

export default class receipt extends Component {
  render() {
    return <div className={styles.receipt}>receipt: print-print</div>
  }
}
