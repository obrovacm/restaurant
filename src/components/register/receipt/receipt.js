import React, { Component } from "react"

import css from "./receipt.module.scss"

export default class receipt extends Component {
  render() {
    return <div className={css.receipt}>receipt: print-print</div>
  }
}
