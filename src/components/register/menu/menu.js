import React, { Component } from "react"

import css from "./menu.module.scss"

export default class Menu extends Component {
  render() {
    return <div className={css.menu}>menu: click-click</div>
  }
}
